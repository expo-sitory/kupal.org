{
  "buildCommand": "cd recipes && npm install && npm run build && mkdir -p ../public/recipes && rm -rf ../public/recipes/* && cp -r dist/* ../public/recipes/",
  "outputDirectory": "public",
  "routes": [
    {
      "src": "/recipes/(.*)",
      "dest": "/recipes/index.html"
    }
  ]
}
